[workspace]
resolver = "2"
members = [
    "crates/core",
    "crates/config",
    "crates/ffi",
]

[workspace.package]
edition = "2024"
license = "MIT"
version = "0.1.0"

[workspace.lints.rust]
unsafe_code = "deny"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
